# 🏗️ Project Architecture Diagram

## System Overview

```
┌─────────────────────────────────────────────────────────────────────┐
│                          YOUR MLOPS PROJECT                          │
│                     Drug Classification System                       │
└─────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────┐
│                         DEVELOPMENT PHASE                            │
└─────────────────────────────────────────────────────────────────────┘

    ┌──────────────┐
    │ drug200.csv  │  Raw Data (200 patient records)
    └──────┬───────┘
           │
           ▼
    ┌─────────────────────────────────────────────────────┐
    │         model_pipeline.py (ML Pipeline)             │
    │                                                      │
    │  ┌────────────┐  ┌─────────────┐  ┌─────────────┐ │
    │  │ Load Data  │→ │ Explore     │→ │ Prepare     │ │
    │  └────────────┘  └─────────────┘  │ - Binning   │ │
    │                                    │ - Encoding  │ │
    │                                    │ - SMOTE     │ │
    │                                    └──────┬──────┘ │
    │                                           │         │
    │  ┌────────────┐  ┌─────────────┐        │         │
    │  │ Evaluate   │← │ Train RF    │←───────┘         │
    │  └─────┬──────┘  │ Model       │                   │
    │        │         └─────┬───────┘                   │
    │        │               │                            │
    └────────┼───────────────┼────────────────────────────┘
             │               │
             │               ▼
             │        ┌──────────────────┐
             │        │  Save Model      │
             │        │  (.pkl file)     │
             │        └────────┬─────────┘
             │                 │
             ▼                 ▼
    ┌────────────────┐  ┌─────────────────────────┐
    │  Confusion     │  │   models/               │
    │  Matrix Plot   │  │   random_forest_        │
    │  (.png)        │  │   model.pkl             │
    └────────────────┘  └───────┬─────────────────┘
                                │
                                │ Trained Model Ready!
                                │
┌───────────────────────────────┼─────────────────────────────────────┐
│                         DEPLOYMENT PHASE                             │
└───────────────────────────────┼─────────────────────────────────────┘
                                │
                                ▼
                   ┌────────────────────────┐
                   │    app.py (FastAPI)    │
                   │   ┌────────────────┐   │
                   │   │ Load Model     │   │
                   │   └────────┬───────┘   │
                   │            │           │
                   │   API Endpoints:       │
                   │   • GET  /health       │
                   │   • POST /predict      │
                   │   • POST /retrain      │
                   │   • GET  /model/info   │
                   │   • GET  /docs         │
                   └────────┬───────────────┘
                            │
                   Port 8000 (REST API)
                            │
        ┌───────────────────┼───────────────────┐
        │                   │                   │
        ▼                   ▼                   ▼
┌───────────────┐  ┌─────────────────┐  ┌──────────────┐
│  Web Browser  │  │   flask_app.py  │  │  test_api.py │
│  Swagger UI   │  │   (Web UI)      │  │  (Testing)   │
│               │  │                  │  │              │
│ /docs         │  │  Templates:      │  │ Automated    │
│ Interactive   │  │  • index.html    │  │ Tests        │
│ Testing       │  │  • predict.html  │  │              │
└───────────────┘  │  • retrain.html  │  └──────────────┘
                   │  • model_info    │
                   │  • about.html    │
                   └─────────────────┘
                   Port 5000 (Web UI)
                            │
                            ▼
                   ┌─────────────────┐
                   │   End Users     │
                   │   - Doctors     │
                   │   - Pharmacists │
                   └─────────────────┘

┌─────────────────────────────────────────────────────────────────────┐
│                    CONTAINERIZATION (Docker)                         │
└─────────────────────────────────────────────────────────────────────┘

        ┌─────────────────────────────────────────────┐
        │          Dockerfile                          │
        │  ┌───────────────────────────────────────┐  │
        │  │ Base Image: python:3.9-slim           │  │
        │  │                                        │  │
        │  │ Contains:                              │  │
        │  │  • Python 3.9                          │  │
        │  │  • All dependencies (requirements.txt) │  │
        │  │  • app.py                              │  │
        │  │  • model_pipeline.py                   │  │
        │  │  • models/                             │  │
        │  │  • drug200.csv                         │  │
        │  │                                        │  │
        │  │ Exposes: Port 8000                     │  │
        │  │ Runs: uvicorn app:app                  │  │
        │  └───────────────────────────────────────┘  │
        └──────────────────┬──────────────────────────┘
                           │
                           │ docker build
                           │
                           ▼
        ┌─────────────────────────────────────────────┐
        │       Docker Image                           │
        │       drug-classification-api:latest         │
        └──────────────────┬──────────────────────────┘
                           │
                           │ docker run / docker-compose up
                           │
                           ▼
        ┌─────────────────────────────────────────────┐
        │       Running Container                      │
        │                                              │
        │  ┌────────────────────────────────────────┐ │
        │  │  Complete Isolated Environment          │ │
        │  │  • OS (Linux)                           │ │
        │  │  • Python                               │ │
        │  │  • All libraries                        │ │
        │  │  • Your code                            │ │
        │  │  • Trained model                        │ │
        │  │                                         │ │
        │  │  Running on: http://localhost:8000     │ │
        │  └────────────────────────────────────────┘ │
        └─────────────────────────────────────────────┘
                           │
                           │ Can be deployed to:
                           │
        ┌──────────────┬───┴───┬──────────────┬───────────┐
        │              │       │              │           │
        ▼              ▼       ▼              ▼           ▼
    ┌────────┐  ┌────────┐ ┌────────┐  ┌────────┐  ┌─────────┐
    │  AWS   │  │ Google │ │ Azure  │  │ Heroku │  │  Local  │
    │  ECS   │  │ Cloud  │ │        │  │        │  │  Server │
    └────────┘  └────────┘ └────────┘  └────────┘  └─────────┘

┌─────────────────────────────────────────────────────────────────────┐
│                     AUTOMATION (Makefile)                            │
└─────────────────────────────────────────────────────────────────────┘

                        ┌──────────────┐
                        │   Makefile   │
                        │              │
                        │  Commands:   │
                        └──────┬───────┘
                               │
        ┌──────────────────────┼──────────────────────┐
        │                      │                      │
        ▼                      ▼                      ▼
┌───────────────┐    ┌──────────────────┐   ┌─────────────────┐
│ make install  │    │  make pipeline   │   │  make serve     │
│               │    │                  │   │                 │
│ pip install   │    │  python main.py  │   │  uvicorn app    │
│ -r require... │    │  --action full   │   │  --reload       │
└───────────────┘    └──────────────────┘   └─────────────────┘
        │                      │                      │
        ▼                      ▼                      ▼
┌───────────────┐    ┌──────────────────┐   ┌─────────────────┐
│  Dependencies │    │  Trained Model   │   │  API Running    │
│  Installed    │    │  Created         │   │  on Port 8000   │
└───────────────┘    └──────────────────┘   └─────────────────┘

┌─────────────────────────────────────────────────────────────────────┐
│                          DATA FLOW                                   │
└─────────────────────────────────────────────────────────────────────┘

User Input → API → Preprocessing → Model → Prediction → Response → User

Step-by-step:

1. User sends JSON:
   {
     "Age": 45,
     "Sex": "M",
     "BP": "HIGH",
     "Cholesterol": "NORMAL",
     "Na_to_K": 15.5
   }

2. FastAPI receives and validates (Pydantic)
   ✓ Age is integer
   ✓ Sex is string
   ✓ All fields present

3. Preprocessing (preprocess_input function):
   Age 45 → Age_binned='40s'
   Na_to_K 15.5 → Na_to_K_binned='10-20'
   One-hot encoding →
   [0, 0, 0, 1, 0, 0, 0, 1, 0, 1, 0, 0, ...]
   18 features total

4. Model prediction:
   RandomForest(100 trees) votes
   → 85 trees vote "DrugY"
   → 10 trees vote "DrugX"
   → 5 trees vote "DrugC"
   → Majority: DrugY

5. Response sent back:
   {
     "prediction": "DrugY",
     "patient_data": {...},
     "status": "success"
   }

┌─────────────────────────────────────────────────────────────────────┐
│                       TECHNOLOGY STACK                               │
└─────────────────────────────────────────────────────────────────────┘

┌─────────────────────┐
│   FRONTEND LAYER    │
├─────────────────────┤
│ • Flask             │  Web framework for UI
│ • HTML/CSS          │  User interface
│ • Bootstrap 5       │  Styling
│ • JavaScript        │  Interactivity
└──────────┬──────────┘
           │
           │ HTTP Requests
           │
┌──────────▼──────────┐
│   APPLICATION LAYER │
├─────────────────────┤
│ • FastAPI           │  REST API framework
│ • Pydantic          │  Data validation
│ • Uvicorn           │  ASGI server
└──────────┬──────────┘
           │
           │ Function calls
           │
┌──────────▼──────────┐
│   ML/DATA LAYER     │
├─────────────────────┤
│ • scikit-learn      │  ML algorithms
│ • pandas            │  Data manipulation
│ • NumPy             │  Numerical computing
│ • imbalanced-learn  │  SMOTE
│ • joblib            │  Model serialization
└──────────┬──────────┘
           │
┌──────────▼──────────┐
│   STORAGE LAYER     │
├─────────────────────┤
│ • File System       │  Store .pkl models
│ • CSV               │  Training data
└─────────────────────┘

┌─────────────────────────────────────────────────────────────────────┐
│                         MLOPS WORKFLOW                               │
└─────────────────────────────────────────────────────────────────────┘

┌─────────────┐
│ 1. DEVELOP  │
│             │  • Write code
│ Local Dev   │  • Train model
└──────┬──────┘  • Test locally
       │
       ▼
┌─────────────┐
│ 2. VERSION  │
│  CONTROL    │  • Git add/commit
│             │  • Push to GitHub
│ Git/GitHub  │  • Track changes
└──────┬──────┘
       │
       ▼
┌─────────────┐
│ 3. TEST     │
│             │  • Run unit tests
│ Automated   │  • API tests
│ Testing     │  • Validate model
└──────┬──────┘
       │
       ▼
┌─────────────┐
│ 4. BUILD    │
│             │  • Docker build
│ Dockerize   │  • Create image
└──────┬──────┘  • Tag version
       │
       ▼
┌─────────────┐
│ 5. DEPLOY   │
│             │  • Push to cloud
│ Production  │  • Start container
└──────┬──────┘  • Monitor
       │
       ▼
┌─────────────┐
│ 6. MONITOR  │
│             │  • Check health
│ Operations  │  • Track metrics
└──────┬──────┘  • Log predictions
       │
       │ (If issues or improvements needed)
       │
       └─────────────┐
                     │
                     ▼
               ┌─────────────┐
               │ 7. ITERATE  │
               │             │  • Fix bugs
               │ Continuous  │  • Improve model
               │ Improvement │  • Update features
               └──────┬──────┘
                      │
                      └─────► Back to Step 1

┌─────────────────────────────────────────────────────────────────────┐
│                    FILE INTERACTION MAP                              │
└─────────────────────────────────────────────────────────────────────┘

drug200.csv
    │
    ├─► model_pipeline.py (loads data)
    │       │
    │       ├─► main.py (calls pipeline functions)
    │       │       │
    │       │       └─► Outputs: models/random_forest_model.pkl
    │       │
    │       └─► app.py (uses pipeline functions for retraining)
    │
    └─► app.py (for retraining)

models/random_forest_model.pkl
    │
    ├─► app.py (loads model at startup)
    │       │
    │       ├─► Serves predictions via /predict
    │       ├─► Provides info via /model/info
    │       └─► Retrains via /retrain
    │
    └─► main.py (saves after training)

app.py (FastAPI)
    │
    ├─► test_api.py (tests endpoints)
    │
    ├─► flask_app.py (consumes API)
    │       │
    │       └─► templates/*.html (renders UI)
    │
    └─► Docker (containerizes)
            │
            └─► docker-compose.yml (orchestrates)

requirements.txt
    │
    ├─► pip install (manual)
    ├─► Makefile → make install
    └─► Dockerfile → RUN pip install

Makefile
    │
    ├─► Triggers: main.py
    ├─► Triggers: app.py
    ├─► Triggers: test_api.py
    └─► Triggers: docker-compose

┌─────────────────────────────────────────────────────────────────────┐
│                     TYPICAL USER JOURNEY                             │
└─────────────────────────────────────────────────────────────────────┘

Scenario: Doctor wants drug recommendation for a patient

1. Doctor opens browser → http://localhost:5000
                          (Flask interface)

2. Clicks "Make Prediction" → navigate to /predict

3. Fills form:
   - Age: 52
   - Sex: Female
   - BP: High
   - Cholesterol: Normal
   - Na/K Ratio: 18.3

4. Clicks "Predict Drug" button

5. Flask sends HTTP POST to → http://localhost:8000/predict

6. FastAPI receives request:
   - Validates data (Pydantic)
   - Calls preprocess_input()
   - Transforms data to model format
   - Calls model.predict()
   - Gets result: "DrugY"

7. FastAPI returns JSON:
   {
     "prediction": "DrugY",
     "patient_data": {...},
     "status": "success"
   }

8. Flask receives response → Renders result page

9. Doctor sees: "Recommended Drug: DrugY" 
   with patient details displayed

10. Doctor prescribes DrugY to patient ✓

┌─────────────────────────────────────────────────────────────────────┐
│                      LEARNING CHECKPOINTS                            │
└─────────────────────────────────────────────────────────────────────┘

□ I understand what each file does
□ I can run the ML pipeline (make pipeline)
□ I can start the API (make serve)
□ I can test the API (make test-api)
□ I can use Swagger UI (http://localhost:8000/docs)
□ I can start the web interface (make flask)
□ I understand how preprocessing works
□ I understand Random Forest algorithm basics
□ I understand REST API concepts (GET, POST)
□ I understand Docker basics
□ I can modify hyperparameters
□ I can add a new API endpoint
□ I can test predictions with different inputs
□ I understand the difference between training and inference
□ I understand why we need train/test split
□ I understand what SMOTE does
□ I can explain the full data flow
□ I understand MLOps principles
□ I can deploy the project in Docker
□ I'm ready to build my own ML project!

┌─────────────────────────────────────────────────────────────────────┐
│                     QUICK COMMAND REFERENCE                          │
└─────────────────────────────────────────────────────────────────────┘

# Setup
make install              # Install dependencies
source venv/bin/activate  # Activate virtual environment

# Training
make pipeline             # Train the model
python main.py --action full_pipeline  # Alternative

# Serving
make serve                # Start FastAPI (port 8000)
make flask                # Start Flask UI (port 5000)

# Testing
make test-api             # Test API endpoints
curl http://localhost:8000/health  # Quick health check

# Docker
make docker-build         # Build Docker image
make docker-run           # Run in container
make docker-stop          # Stop container

# Maintenance
make clean                # Remove temporary files
make help                 # Show all commands

# URLs
http://localhost:8000/docs      # API documentation
http://localhost:8000/health    # Health check
http://localhost:5000           # Web interface

┌─────────────────────────────────────────────────────────────────────┐
│                          KEY TAKEAWAYS                               │
└─────────────────────────────────────────────────────────────────────┘

1. DATA → MODEL → API → USER (Complete ML pipeline)

2. Separation of concerns:
   - model_pipeline.py = ML logic
   - app.py = API layer
   - flask_app.py = UI layer

3. Automation through Makefile (one command = multiple steps)

4. Containerization = Deploy anywhere

5. Testing = Confidence in production

6. Documentation = Team collaboration

7. This is production-ready MLOps! 🚀

┌─────────────────────────────────────────────────────────────────────┐
│                           NEXT STEPS                                 │
└─────────────────────────────────────────────────────────────────────┘

1. Run through each component manually
2. Modify hyperparameters and observe changes
3. Add your own features
4. Deploy to a cloud service
5. Add monitoring and logging
6. Implement CI/CD pipeline
7. Share your project on GitHub
8. Build your own ML project from scratch!

Remember: The best way to learn is by doing! 💪
